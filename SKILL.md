---
name: style-mimic
description: "基于文体学（stylistics）与语料库语言学的文风模仿：从样本提取可复用的风格档案（词汇/句法/修辞/语篇/声音五层，定量指标 + 质性描写），再用档案约束新写作并量化校验偏差。Use when 用户要求模仿某位作者/某本教材/某种文体的文风、语句风格、修辞风格、句式（文风模仿、照……的风格写、style mimic、voice profile、文体分析），或要把一段文字改写成指定风格。"
---

# style-mimic — 文体学驱动的文风模仿

核心思想：**文风不是感觉，是一组可测量、可枚举的语言选择**（Leech & Short 1981）。本 skill 把"模仿文风"拆成三步可验证的流程：取样分析 → 建风格档案 → 按档案写作并用脚本校验偏差。

**预期管理**（2024–2025 实证）：结构化文体（学术/新闻/邮件）模仿效果好，非正式文体（博客/论坛）难；风格判断的人评一致性本身只有 0.64，目标是"分布落入区间"，不是 100% 保真。

## 理论依据（动笔前必读）

分析维度与指标定义全部来自 [references/style-dimensions.md](references/style-dimensions.md)，框架依据：

- Leech & Short (1981)《Style in Fiction》四层清单：词汇 / 语法 / 修辞格 / 衔接与语境
- Biber (1988) 形态句法特征集（每千词频率，register 分析标准做法）
- Hyland (2005) stance & engagement 模型（hedge/booster/self-mention/reader pronoun/directive）
- Ure (1971) 词汇密度；连淑能 (1993) 形合/意合对比（中英文案关键差异）
- 近期验证：Brown et al. (2024, arXiv:2410.16107) 用 Biber 特征集稳定区分写作风格

## 工作流程

### 第 1 步：取样

- 样本要**纯**：同一作者/同一文体，剔除目录、参考文献、他人引文；中英分开取样。
- 样本量底线：中文 ≥ 3000 字 / 英文 ≥ 2000 词；能多文档就多文档（风格是分布，不是单点）。

### 第 2 步：定量分析（脚本）

```bash
python3 scripts/style_analyze.py sample1.txt sample2.txt            # 各样本指标
python3 scripts/style_analyze.py sample.txt draft.txt --compare     # 成品与样本偏差对照
```

脚本输出：句长分布（均值/标准差/p10–p90/分桶）、每句小句数（形合度）、句类比例、TTR、五类标记每千单位频率、高频实词。安装 jieba 后中文高频词更准确（可选，非必需）。

### 第 3 步：质性分析（人工/模型阅读样本）

按 style-dimensions.md 的六层清单逐层观察并记录（叙事文本必须做第 6 层"叙事声音"），重点是脚本算不出的：修辞格的具体用法与密度、类比的一贯手法、段落推进模式、开头结尾习惯、介入读者的方式。每层至少摘 2 个原文例句（Claim–Evidence 对）。同时产出一份"平均作者基线"（同题通用文本），档案只记差分特征。

### 第 4 步：建风格档案

把定量结果 + 质性观察填进 [references/profile-template.md](references/profile-template.md)，存为 `profiles/<名称>.md`。档案必须可执行：每条观察落成"写作时做什么/不做什么"。

**声纹与体裁分离**（Biber & Conrad 2009）：档案只装**作者声纹**（voice）——稳定的语言选择习惯，跨体裁不变；目标文本的**体裁约束**（register/genre）——讲义要有自测、书章可以有长轶事、公众号要有钩子——单独写进档案的"体裁校准"节。写作时两层叠加：体裁决定**结构**（有什么零件），声纹决定**措辞**（零件怎么说话）。同一个 Oakley 档案既能产书章也能产讲义：讲义版把"开场轶事"压缩到两句、把直接指令移进自测题，声纹指标（句长节奏、比喻密度、人称）保持不变。校验时 `--compare` 只对声纹指标，体裁零件按清单人工核。

### 第 5 步：按档案写作 + 校验

- 写作时把档案的"硬约束"（句长区间、标记频率区间、禁用句式）放在手边。
- 初稿完成后跑 `--compare`：任何标记频率偏差超过 2 倍或句长分布明显漂移的项，逐项修正后重跑，直到指标落入样本区间。
- 最后一遍人工读：修辞和语篇节奏只能人耳校验。

## 红线

- 不以"感觉像"代替档案：没有档案不动笔。
- 不混合取样：作者 A 的档案不能从 A+B 的混合文本提取（可跑多次分析看文档间指标是否聚在一起来自检）。
- 指标区间≠逐句对齐：风格是统计分布，允许单句偏离，不允许整体漂移。
- 档案优先于通用写作建议：样本里作者就是爱用破折号，那"少用破折号"之类的通用戒律自动作废。

## 失败模式排查（成品定稿前逐项过）

1. **滑向平均腔**：非正式文体尤其易发；表现是"像所有人，不像他"——对照差分节逐项查。
2. **方差塌缩**：句长/段落的变化幅度被熨平（脚本已自动报警：SD 比值 < 0.7）。
3. **照抄输入**：输出与样本逐句重合度高 = 没迁移只复制。
4. **体裁冒充声纹**：把"讲义都有自测题"这类体裁惯例当成作者个人风格写进档案。
5. **微特征丢失**：省略号、大小写癖、口头禅、标点癖——逐项对照标点指纹与代词向量。
6. **AI 腔回流**：按 Wikipedia "Signs of AI writing" 模式清单查（注意清单有时效性，按模式簇判断，不数单个词）。

## 伦理边界（Mukherjee et al. 2024）

模仿活人作者的可识别风格用于公开发表时：声明仿写关系、不冒充本人、注意人格权与版权；教学/私人练习用途从宽。skill 产出默认视为"风格练习"，不用于伪造出处。

## 可选：模仿练习分级（教学用法）

按 Taylor / Liang 的教学阶梯把任务分级：① 抄写+标注范本特征 → ② 微变换（换主语/语态/受众）→ ③ 受控仿写（换内容保形式）→ ④ pastiche（同一作者的新场景）→ ⑤ 脱仿（只留约束清单）。每级都要求先输出"范本特征分析"再动笔。
